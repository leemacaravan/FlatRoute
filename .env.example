# Tile provider API key (e.g. Stadia Maps, Protomaps)
# Leave blank to use OpenFreeMap (no key needed)
VITE_TILE_KEY=

# OpenRouteService API key (for routing + geocoding — added in a later phase)
VITE_ORS_KEY=
